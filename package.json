{
  "name": "campus-quest",
  "version": "1.0.0",
  "private": true,
  "description": "Campus Quest full-stack gamified student challenge platform.",
  "scripts": {
    "setup": "npm install && npm install --prefix server && npm install --prefix client",
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
